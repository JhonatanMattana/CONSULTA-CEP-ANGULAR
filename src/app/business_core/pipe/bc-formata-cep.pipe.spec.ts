import { BcFormataCepPipe } from './bc-formata-cep.pipe';

describe('BcFormataCepPipe', () => {
  it('create an instance', () => {
    const pipe = new BcFormataCepPipe();
    expect(pipe).toBeTruthy();
  });
});
